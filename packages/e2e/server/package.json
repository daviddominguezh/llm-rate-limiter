{
  "name": "@llm-rate-limiter/e2e-server",
  "version": "1.0.0",
  "description": "E2E proxy server that load balances between server instances",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist tsconfig*.tsbuildinfo && tsc -p tsconfig.build.json",
    "typecheck": "tsc -b",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts"
  },
  "private": true,
  "license": "MIT",
  "dependencies": {
    "dotenv": "^17.2.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
