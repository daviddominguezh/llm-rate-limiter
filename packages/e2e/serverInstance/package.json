{
  "name": "@llm-rate-limiter/e2e-server-instance",
  "version": "1.0.0",
  "description": "E2E test server instance for LLM rate limiter",
  "main": "dist/main.js",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist tsconfig*.tsbuildinfo && tsc -p tsconfig.build.json",
    "typecheck": "tsc -b",
    "start": "node dist/main.js",
    "dev": "tsx src/main.ts --watch"
  },
  "private": true,
  "license": "MIT",
  "dependencies": {
    "@llm-rate-limiter/core": "^1.0.0",
    "@llm-rate-limiter/redis": "^1.0.0",
    "dotenv": "^17.2.3",
    "express": "^5.0.0",
    "ioredis": "^5.4.1",
    "winston": "^3.19.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/express": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
